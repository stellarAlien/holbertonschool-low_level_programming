#include "main.h"
d iu function that prints every other
 *character of a string, starting with the first
 *character, followed by a new line.
 *@str: pointer char
 *Return: void
 */ts2(char *str)
{
int i;
while ( str[i] != '\0')
{
putchar(str[i]);
i++;
}
}
