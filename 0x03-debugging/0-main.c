#include "main.h"

/**
 * * main - tests function that prints if integer is positive or negative
 * * Return: 0
 * */

int main(void)
{
	        int i;
		i = 0;
			 if ( positive_or_negative(i) == 0)
			 {
				printf("0 is zero");
				 return(1);
			 }
			 else
			 {
		 		 return (0);
}
}
